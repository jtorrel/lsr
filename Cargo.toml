[package]
    name = "lsr"
    description = "A Rust implementation of ls"
    version = "0.1.0"
    edition = "2024"

[dependencies]
    clap = { version = "4.6.2", features = ["derive"] }

[dev-dependencies]
    tempfile = "3.27.0"
